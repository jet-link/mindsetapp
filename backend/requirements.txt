# --- Core ---
Django>=5.2,<6
djangorestframework>=3.15
djangorestframework-simplejwt>=5.4
drf-spectacular>=0.28
django-cors-headers>=4.6

# --- Auth (social: Google/Apple подключаем позже через allauth) ---
django-allauth[socialaccount]>=65,<66

# --- DB / Cache / Queue ---
psycopg[binary]>=3.2
redis>=5.0
django-redis>=5.4
celery>=5.4

# --- Content pipeline ---
bleach>=6.0
Pillow>=10.0
# Видео: постер (первый кадр) + метаданные извлекаются через системные
# бинарники ffmpeg/ffprobe (вызываются как subprocess; отдельный pip-пакет не
# нужен). Установка: macOS `brew install ffmpeg`, Debian/Ubuntu
# `apt-get install ffmpeg`. Без ffmpeg видео всё равно загружается, но без
# серверного постера (фронт показывает первый кадр через <video preload>).

# --- Tooling ---
python-dotenv>=1.0
gunicorn>=22.0
